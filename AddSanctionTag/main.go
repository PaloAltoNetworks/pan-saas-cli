package main

func main() {
	/*
	   curl -k -X GET "https://{FIREWALL_IP}/api/?key={FWAPIKEY}&type=config&action=set&xpath=/config/devices/entry[@name='localhost.localdomain']/vsys/entry[@name='vsys1']/application-tag/entry[@name='zoom-base']/tag&element=<member>Sanctioned</member>"

	*/
}
